# v2
spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 20MB

logging:
  level:
    com:
      amazonaws:
        util:
          EC2MetadataUtils: error
    root: info
    software.amazon.awssdk: debug


cloud:
  aws:
    s3:
      bucketName:    # bucket name
      role-arn:      # role arn
      role-session:  # role session name
      duration:      # duration